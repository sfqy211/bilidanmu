# 页面设计

> 管理面板（4 个子页面）、弹幕页面、系统托盘的 UI 规范与交互细节。

---

## 一、页面总览

两类页面：**管理面板**（侧边栏 + Tab 内容区）、**弹幕页面**（独立全屏）。

```
┌─────────────────────────────────────────────────────────────────┐
│                         BiliDanmu                               │
├─────────────┬───────────────────────────────────────────────────┤
│             │                                                   │
│  管理面板    │  弹幕页面（独立全屏布局）                           │
│  (4 个子页面│  (从管理面板子页面一「打开弹幕」进入)                │
│   通过 Tab  │                                                   │
│   切换)     │  ┌─────────────────────────────────────────────┐ │
│             │  │          实时弹幕流 + 发送栏                  │ │
│  ┌──┐       │  │  弹幕列表 + 表情选择器 + 自动发送面板           │ │
│  │📺│       │  └─────────────────────────────────────────────┘ │
│  │👤│       │                                                   │
│  │🤖│       │                                                   │
│  │⚙│       │                                                   │
│  ├──┤       │                                                   │
│  │v0│       │                                                   │
│  └──┘       │                                                   │
├─────────────┴───────────────────────────────────────────────────┤
│                  系统托盘（运行期间常驻）                         │
│                  单击切换弹幕窗口 / 右键弹出菜单                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、路由结构

```
/                           → 重定向到 /rooms
/rooms                      → 管理面板 - 直播间（Tab: 已添加 / 搜索）
/accounts                   → 管理面板 - 账号（Tab: 登录 / 账号）
/ai                         → 管理面板 - AI 接入（Tab: 添加模型 / 已保存）
/ai-assistant               → 管理面板 - AI 助手
/settings                   → 管理面板 - 设置（Tab: 弹幕发送 / 弹幕接收 / 外观 / 音频 / 语音识别 / 缓存管理）
/room/:roomId               → 弹幕页面（独立全屏布局）
```

```tsx
<Routes>
  <Route element={<AppLayout />}>
    {/* 侧边栏(w-16) + 内容区 */}
    <Route path="/rooms" element={<RoomPage />} />
    <Route path="/accounts" element={<AccountPage />} />
    <Route path="/ai" element={<AIPage />} />
    <Route path="/ai-assistant" element={<AiAssistantPage />} />
    <Route path="/settings" element={<SettingsPage />} />
  </Route>
  <Route path="/room/:roomId" element={<DanmakuPage />} /> {/* 独立布局 */}
</Routes>
```

---

## 三、管理面板

**整体布局：** 左侧 64px 图标侧边栏 + 右侧内容区

```
┌──────────────────────────────────────────────────────┐
│  TitleBar: [icon] BiliDanmu            ─  □  ✕     │
├────────────┬─────────────────────────────────────────┤
│            │                                         │
│  📺 直播间 │        页面标题 + 描述                    │
│  👤 账号   │        ──────────────────────            │
│  🤖 AI    │        [Tab A] [Tab B]                   │
│  ⚙️ 设置  │        ┌─────────────────────────────┐   │
│            │        │                             │   │
│────────────│        │     Tab 内容区               │   │
│ 版本号     │        │                             │   │
└────────────┴─────────────────────────────────────────┘
  侧边栏 w-16     内容区自适应
  (图标 + 左侧     (页面标题 + PageTabs + 内容)
   粉色竖条指示)
```

**TitleBar 规范：**

- 自定义 TitleBar 组件，左侧应用图标 + "BiliDanmu" 文本，右侧最小化/最大化/关闭按钮
- 支持窗口拖动

**侧边栏规范：**

- 宽度 `w-16`（64px），顶部无品牌标识，直接显示导航项，仅显示图标，hover 显示文字 tooltip
- 当前选中项：左侧 3px 粉色竖条 + 背景色变化
- 底部：版本号
- 4 个导航项：直播间、账号、AI 接入、设置

**页面通用模式：**

所有管理页面共享相同结构：

1. 页面标题（`text-2xl font-semibold`）+ 灰色描述文案
2. 右上角操作按钮 / 状态提示（error/success）
3. `PageTabs`（Radix Tabs）切换内容区域
4. 内容区使用统一的边框卡片样式（`border-slate-300 bg-white` / 暗色 `border-white/[0.06] bg-[#12141e]`）

---

### 3.1 子页面一：直播间（RoomPage）

**Tab 结构：** `已添加 (N)` | `搜索`

```
┌─────────────────────────────────────────────────────────┐
│  直播间                                                  │
│  添加、管理并切换当前使用的直播间。                        │
├────────────┬────────────────────────────────────────────┤
│ 已添加 (2)  │ 搜索                                       │
├────────────┴────────────────────────────────────────────┤
│                                                         │
│  ──── 搜索 Tab ────                                     │
│  ┌──────────────────────────────────────────────────┐   │
│  │ [主播名字 ▼]  🔍 输入主播名字搜索直播间    [搜索] │   │
│  └──────────────────────────────────────────────────┘   │
│  搜索结果 (3 个)                                        │
│  ┌──────────────────┐ ┌──────────────────┐             │
│  │ 🔴 主播A  12345  │ │ 🔴 主播B  67890  │             │
│  │ 今晚继续冲！  [+] │ │ 休息一天    [+]  │             │
│  └──────────────────┘ └──────────────────┘             │
│                                                         │
│  ──── 已添加 Tab ────                                   │
│  ┌──────────────────────────────────────────────────┐   │
│  │ 🔴 主播A  12345  [当前]                           │   │
│  │ 直播间标题 直播中                                │   │
│  │ [设为当前] [打开弹幕] [🗑]                         │   │
│  ├──────────────────────────────────────────────────┤   │
│  │ ⚫ 主播B  67890                                   │   │
│  │ 休息一天 · 未开播                                  │   │
│  │ [设为当前] [打开弹幕] [🗑]                         │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

**交互细节：**

1. 搜索模式下拉选择：主播名字 / 直播间号 / 直播链接 / UID，placeholder 随模式变化
2. 搜索结果以 2 列网格展示，每条显示直播状态圆点、主播名、标题、房间号、添加按钮
3. 已添加房间以 2 列网格卡片展示，当前房间粉色高亮 + "当前" 标签
4. 操作按钮：打开弹幕（自动设为当前 + 持久化 + `open_danmaku_window`）、删除（若删的是当前房间，同步清理持久化 currentRoomId）
5. 删除当前房间时自动保存 `selections.save({ currentRoomId: null })`

---

### 3.2 子页面二：账号（AccountPage）

**Tab 结构：** `登录` | `账号`

```
┌─────────────────────────────────────────────────────────┐
│  账号                                                    │
│  扫码或 Cookie 登录，管理多个账号。                       │
├────────┬────────────────────────────────────────────────┤
│ 登录   │ 账号                                           │
├────────┴────────────────────────────────────────────────┤
│                                                         │
│  ──── 登录 Tab（左右双栏） ────                          │
│  ┌─────────────────────┐ ┌─────────────────────────┐   │
│  │ 扫码登录             │ │ Cookie 登录             │   │
│  │ ┌─────────────┐     │ │ ┌───────────────────┐  │   │
│  │ │  QR 二维码   │     │ │ │  粘贴完整 Cookie  │  │   │
│  │ │  (本地生成)  │     │ │ │  字符串...         │  │   │
│  │ └─────────────┘     │ │ └───────────────────┘  │   │
│  │ [生成二维码]         │ │ [使用 Cookie 登录]      │   │
│  │ 请使用 App 扫码...   │ │ 需包含 SESSDATA         │   │
│  └─────────────────────┘ └─────────────────────────┘   │
│                                                         │
│  ──── 账号 Tab ────                                     │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 头像  用户A  [当前]  UID: 123456                  │   │
│  │                    [切换] [移除]                  │   │
│  ├─────────────────────────────────────────────────┤   │
│  │ 头像  用户B             UID: 789012              │   │
│  │                    [切换] [移除]                  │   │
│  └─────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 快捷操作                              [退出当前] │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

**交互细节：**

1. **扫码登录**：点击「生成二维码」→ `qrcode` 库本地渲染 → 每 3s 轮询 `poll_qr` → 过期自动刷新 → 成功后写入 auth-store
2. **Cookie 登录**：粘贴完整 Cookie 字符串 → textarea 输入 → 提示需包含 `SESSDATA` 与 `bili_jct`
3. **多账号列表**：头像、用户名、UID、当前标记；切换/移除按钮
4. **移除账号**：后端自动激活下一个账号，返回新活跃 ID 前端同步
5. **退出登录**：返回剩余账号列表，前端用列表更新而非清空
6. 托盘菜单同步显示所有账号，CheckMenuItem 切换

---

### 3.3 子页面三：AI 接入（AIPage）

**Tab 结构：** `添加模型` / `编辑模型` | `已保存 (N)`

```
┌─────────────────────────────────────────────────────────┐
│  AI 接入                                                 │
│  OpenAI 兼容接口的配置、测试与模型切换。                   │
├────────────┬────────────────────────────────────────────┤
│ 添加模型    │ 已保存 (2)                                  │
├────────────┴────────────────────────────────────────────┤
│                                                         │
│  ──── 添加/编辑模型 Tab ────                             │
│  ┌──────────────────────────────────────────────────┐   │
│  │ 添加模型                          [取消编辑]      │   │
│  │ Endpoint [https://api.openai.com/v1]             │   │
│  │ API Key  [sk-••••••••]                           │   │
│  │ 模型名   [gpt-4o-mini]                           │   │
│  │ 备注     [我的 GPT-4o 账号]                       │   │
│  │                                                  │   │
│  │ 可用模型: [gpt-4o-mini] [gpt-4o] [gpt-3.5]      │   │
│  │                                                  │   │
│  │ [获取模型列表] [测试连接] [保存模型]               │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│  ──── 已保存 Tab ────                                   │
│  ┌──────────────────┐ ┌──────────────────┐             │
│  │ gpt-4o-mini [当前]│ │ qwen2.5:7b      │             │
│  │ api.openai.com   │ │ localhost:11434  │             │
│  │ 我的 GPT-4o 账号 │ │                  │             │
│  │ [编辑][设为当前]  │ │ [编辑][设为当前]  │             │
│  │ [删除]           │ │ [删除]           │             │
│  └──────────────────┘ └──────────────────┘             │
└─────────────────────────────────────────────────────────┘
```

**交互细节：**

1. **添加/编辑共享表单**：同一 Tab，编辑时标题切换为「编辑模型」+ 显示「取消编辑」按钮
2. 编辑模型时 API Key 可留空（不会覆盖已保存的密钥），有提示文案
3. **获取模型列表**：调用 `/v1/models` 获取可用模型名，以可点击标签形式展示，点击即填入模型名输入框
4. **测试连接**：调用 `test_ai_connection`，结果以 cyan 色文字显示在页面顶部（如「连接成功 (328ms)」）
5. **已保存列表**：2 列网格卡片，当前模型粉色高亮 + "当前" 标签；操作：编辑、设为当前、删除
6. ID 使用时间戳 + 随机 hex 防冲突，持久化至 SQLite

---

### 3.4 子页面四：设置（SettingsPage）

**Tab 结构：** `弹幕发送` | `弹幕接收` | `外观` | `音频` | `语音识别` | `缓存管理`

```
┌─────────────────────────────────────────────────────────┐
│  设置                                         [保存设置] │
│  发送、接收与外观的核心设置。                              │
├────────┬──────┬──────┬──────┬──────────┬────────────────┤
│弹幕发送│弹幕接收│ 外观  │ 音频  │ 语音识别  │ 缓存管理      │
├────────┴──────┴──────┴──────┴──────────┴────────────────┤
│                                                         │
│  ──── 弹幕发送 ────                                     │
│  ┌──────────────────────────────────────────────────┐   │
│  │ 最小间隔（秒） [1.5]     最大间隔（秒） [3.0]     │   │
│  │ ☑ 启用随机间隔                                    │   │
│  │ ☑ 启用间隔抖动                                    │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│  ──── 弹幕接收 ────                                     │
│  ┌──────────────────────────────────────────────────┐   │
│  │ ☑ 自动连接弹幕流    ☑ 断线自动重连                │   │
│  │ 重连间隔（秒） [5]   最大重连间隔（秒） [60]       │   │
│  │ ☑ 禁言提醒    ☑ Cookie 过期提醒                   │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│  ──── 外观 ────                                         │
│  ┌──────────────────────────────────────────────────┐   │
│  │ 主题  [跟随系统 ▼]                                │   │
│  │ 弹幕字号 [14]                                     │   │
│  │ ☑ 显示勋章                                        │   │
│  │ 弹幕窗口透明度 [90] (0-100)                       │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│  ──── 音频 ────                                         │
│  ┌──────────────────────────────────────────────────┐   │
│  │ （音频相关设置）                                   │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│  ──── 语音识别 ────                                     │
│  ┌──────────────────────────────────────────────────┐   │
│  │ ☐ 启用语音识别                                    │   │
│  │ 模型  [large ▼]                                   │   │
│  │ 同步延迟 [0] ms  (-2000 ~ +2000)                  │   │
│  └──────────────────────────────────────────────────┘   │
│                                                         │
│  ──── 缓存管理 ────                                     │
│  ┌──────────────────────────────────────────────────┐   │
│  │ [清除图片缓存]                                    │   │
│  │ [清除房间专属表情]                                 │   │
│  │ [清除所有缓存]                                    │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

**配置项清单：**

| 配置项         | 类型   | 默认值   | 说明                        | 所在 Tab |
| -------------- | ------ | -------- | --------------------------- | -------- |
| 发送最小间隔   | number | 1.5s     | 两次发送之间的最小间隔      | 弹幕发送 |
| 发送最大间隔   | number | 3.0s     | 两次发送之间的最大间隔      | 弹幕发送 |
| 随机间隔       | bool   | true     | 在 min-max 间随机选择间隔   | 弹幕发送 |
| 微扰           | bool   | true     | 间隔 ±0.3s 抖动             | 弹幕发送 |
| 自动连接弹幕流 | bool   | true     | 进入直播间自动连接 WS       | 弹幕接收 |
| 断线重连       | bool   | true     | 弹幕流断线自动重连          | 弹幕接收 |
| 重连间隔       | number | 5s       | 初始重连间隔                | 弹幕接收 |
| 最大重连间隔   | number | 60s      | 退避策略上限                | 弹幕接收 |
| 禁言提醒       | bool   | true     | 禁言时弹出通知              | 弹幕接收 |
| Cookie过期提醒 | bool   | true     | Cookie 即将过期时提醒       | 弹幕接收 |
| 主题           | enum   | 跟随系统 | 浅色/深色/跟随系统          | 外观     |
| 弹幕字号       | number | 14px     | 弹幕流中文字大小            | 外观     |
| 显示勋章       | bool   | true     | 是否显示粉丝勋章            | 外观     |
| 弹幕窗口透明度 | number | 90       | 弹幕窗口背景透明度 (0-100)  | 外观     |
| 启用语音识别   | bool   | false    | 是否启用 STT（仅 STT 构建） | 语音识别 |
| STT 模型       | enum   | "large"  | 模型 ID（仅 STT 构建）      | 语音识别 |
| 同步延迟       | number | 0ms      | 字幕显示偏移（仅 STT 构建） | 语音识别 |

> **运行时开关**：STT 和 AI 功能始终编译进二进制，用户可在设置中按需开启/关闭。
>
> **未接入前端但已在 Settings 类型中定义的字段：** `riskControl.autoPauseOnMute`、`riskControl.appendRandomSuffix`、`appearance.showLevel`、`notification.sendSuccess`、`notification.scAlert` — 这些在 SettingsPage 中没有对应的 UI 控件。

---

## 四、弹幕页面（DanmakuPage）

**独立窗口布局**，无侧边栏，进入房间后自动连接弹幕流。分区布局：标题栏、控制栏、礼物栏、弹幕栏、功能栏、输入栏。所有背景使用 `danmaku-bg-*` CSS 类支持透明度，各区域之间无边框。

```
┌──────────────────────────────────────────────────────────────────┐
│  TitleBar: {主播名} - {直播标题}   [Pin] [透传] [✕] [退出]      │
│  (可拖动)                                                         │
├──────────────────────────────────────────────────────────────────┤
│  控制栏: [播放/暂停] [音量(hover弹出竖向滑块)] 统计信息           │
│  统计: 弹幕数 | 礼物总额(SC数/舰长数) | 开播时长 | 在线人数 | 点赞│
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│   礼物栏                                                         │
│   (礼物 + 醒目留言 + 上舰消息)                                   │
│   默认文本: "送礼、醒目留言、上舰消息等显示在这里"                 │
│                                                     [回到底部 ↓] │
│                                                                  │
├─ ─ ─ ─ ─ ─ ─ ─ 1px分割线(8px命中区) ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┤
│                                                                  │
│   弹幕栏                                                         │
│   (弹幕 + 系统消息)                                              │
│   默认文本: "本场直播的弹幕互动消息将显示在这里"                   │
│   [勋章] 用户A: 哈哈哈                                           │
│   [勋章] 用户B: [emote_img]                                      │
│   💬 用户C 大表情                                                │
│   ┌──────────────────────────────────────────────────────────┐   │
│   │ 💰 SC 卡片：头像 + 用户名 + ¥30                          │   │
│   │ 底部：SC 消息内容                                        │   │
│   └──────────────────────────────────────────────────────────┘   │
│                                                     [回到底部 ↓] │
│                                                                  │
├──────────────────────────────────────────────────────────────────┤
│  功能栏: [⚡自动发送] [😊表情] [账号切换] [⚙设置] [🤖AI]        │
│  (图标按钮，无文字，hover 显示 tooltip)                          │
│  设置面板: 浮动在功能栏上方，含透明度 slider                      │
├──────────────────────────────────────────────────────────────────┤
│  输入栏: [textarea] [发送]                                       │
│  (无边框)                                                        │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ 表情选择器 / 自动发送面板（浮动在输入栏上方）               │   │
│  └──────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────┘
```

**消息分流：**

| 消息类型             | 分配栏位 | 渲染方式                             |
| -------------------- | -------- | ------------------------------------ |
| 礼物 (`gift`)        | 礼物栏   | `🎁 用户 送出 礼物名`，amber 金色    |
| 醒目留言 (`superChat`)| 礼物栏  | SuperChatCard 两段式卡片（真实颜色） |
| 上舰 (`guard_buy`)   | 礼物栏   | 上舰消息样式                         |
| 弹幕 (`danmaku`)     | 弹幕栏   | `[勋章] 用户名: 消息文本`            |
| 系统 (`system`)      | 弹幕栏   | 系统消息样式                         |

**标题栏：** 自定义标题栏，显示 `{主播名} - {直播标题}` 文本，右侧 Pin 切换、Passthrough 透传切换、Hide(✕) 隐藏、Exit(LogOut) 退出按钮。整个标题栏可拖动窗口。

**控制栏：** 包含音频播放/暂停按钮、音量控件（hover 弹出竖向滑块）、统计信息（弹幕计数、礼物总额含 SC/舰长计数、开播时长、在线人数、点赞数）。

**分割栏：** 1px 细线，8px 命中区域用于拖动调整礼物栏/弹幕栏高度比例，默认 35:65。拖到 <5% 只显示弹幕，拖到 >95% 只显示礼物。比例持久化到 localStorage。

**功能栏：** 图标按钮（无文字），hover 显示 tooltip。包含：自动发送(Zap 图标)、表情(Smile 图标)、账号切换(文字图标)、设置(Settings 图标)、AI(Bot 图标)。设置面板浮动在功能栏上方，含透明度 slider。

**输入栏：** textarea + 发送按钮，无边框。

**弹幕输入栏：**

- 左侧自动发送按钮（🔁）→ 弹出 AutoSendPanel 浮动面板
- 表情按钮（😊）→ 弹出 EmoticonPickerPanel 浮动面板
- 中间单行输入框，Enter 发送，最多 20 字
- 右侧发送按钮（➤），发送中 disabled

**表情选择器（EmoticonPickerPanel）：**

- 标题 + 关闭按钮
- 表情包横向滚动 Tab（封面图标 + 包名 tooltip），`makePkgKey(pkgId-pkgType)` 复合 key 切换
- 下方表情网格（3-6 列响应式），不可用表情灰显 + cursor-not-allowed
- 所有图片通过 `ProxiedImage` 组件自动代理 B 站 CDN

**自动发送面板（AutoSendPanel）：**

- 四个 Tab：文字 / 表情 / 收藏夹 / 点赞
- 文字 Tab：多行 textarea（每行一条），收藏夹可填充到文字
- 表情 Tab：表情选择器 + 已选预览
- 收藏夹 Tab：多弹幕组管理（添加/删除/编辑）
- 点赞 Tab：目标总数/批次/间隔参数 + 启停
- 共享控件：间隔（秒）+ 时间限制（分钟）+ 启动/停止按钮
- 运行状态：已发送 N 条 / 当前第 M 条 / 停止原因
- 切换房间或组件卸载时自动停止（`isRunningRef` 条件检查）

**弹幕流渲染规则：**

| 消息类型       | 渲染方式                             | 样式                |
| -------------- | ------------------------------------ | ------------------- |
| 普通弹幕       | `[勋章] 用户名: 消息文本`            | slate 文字          |
| 含文字表情弹幕 | `[emote_key]` 替换为 inline `<img>`  | InlineEmotText 混排 |
| 大表情弹幕     | 仅表情图片（official\_ 限制 56px）   | 居中 inline         |
| 醒目留言 (SC)  | SuperChatCard 两段式卡片（真实颜色） | 独立卡片            |
| 礼物消息       | `🎁 用户 送出 礼物名`                | amber 金色          |
| 上舰消息       | 上舰消息样式                         | 礼物栏              |

**图片代理：**

所有 B 站 CDN 图片（`*.hdslb.com`）通过 `ProxiedImage` → `useProxyImage` → `proxy_image` IPC 命令代理加载，绕过 Referer 防盗链。代理后的 base64 data URL 缓存在前端 LRU（200 条），代理失败时 fallback 到原始 URL。

**音频控制栏：** 底部输入栏上方，包含播放/暂停按钮、音量滑块、静音按钮。右侧显示开播时长（`Clock` 图标 + `XhXm` 格式，每 5 分钟刷新 `get_live_time`，每分钟更新显示）、在线人数、点赞总数。点击播放自动调用 `get_audio_stream_url` 获取代理 URL，mpegts.js 连接本地代理播放 FLV 音频流。切房时先 clear 再 destroy，自动重连 3 次（4s 间隔）。

**字幕叠加层（SubtitleOverlay）：** 覆盖在弹幕流区域底部，半透明黑底白字，显示 STT 实时识别结果。非最终结果时显示为"正在说话"状态，最终结果后淡出 800ms，5 秒后消失。通过 `useSttTranscript` hook 管理延迟缓冲和按需 RAF 循环。

---

## 五、系统托盘

**图标状态：**

| 应用状态       | 图标 | Tooltip                     |
| -------------- | ---- | --------------------------- |
| 未连接直播间   | 灰色 | BiliDanmu - 未连接          |
| 已连接弹幕流   | 蓝色 | BiliDanmu - 直播间名        |
| 自动发送运行中 | 绿色 | BiliDanmu - 自动发送运行中  |
| 禁言状态       | 红色 | BiliDanmu - 已禁言 (剩余Xm) |

**托盘交互：**

- **单击**：切换弹幕窗口（已显示则隐藏，已隐藏则显示；若无弹幕窗口则显示主窗口）
- **双击**：显示主窗口（250ms 延迟区分单击/双击）
- **右键**：弹出上下文菜单

**右键菜单：**

```
┌──────────────────────────────┐
│  BiliDanmu                   │
│  ──────────────────────────── │
│  直播间                       │
│    ☑ 主播A                    │
│    ☐ 主播B                    │
│  ──────────────────────────── │
│  账号                         │
│    ☑ 用户A                    │
│    ☐ 用户B                    │
│  ──────────────────────────── │
│  AI 模型                      │
│    ☑ GPT-4o Mini              │
│    ☐ qwen2.5                  │
│  ──────────────────────────── │
│  显示主窗口                    │
│  退出                         │
└──────────────────────────────┘
```

Rust 实现要点：所有子菜单统一使用 CheckMenuItem，无图标前缀。直播间从 SQLite 加载 + selections 读取 currentRoomId 确定勾选。账号从 AppState 读取。AI 从 SQLite 加载。点击房间：设为当前 + 刷新托盘 + emit `room-switched`。点击账号：调用 `switch_account` + emit `account-switched`。前端 App.tsx 监听事件同步 Zustand 状态。
